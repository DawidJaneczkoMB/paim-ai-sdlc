All rules and skills should be prefixed for better scoping. This will make it easier to understand what parts of the monorepo are they responsible for, and what they are not.

Prefixes should be:

- `common-`
- `backend-`
- `frontend-`
- `devops-`

Prefixes are self-explaining.
